import { NutritionPlan } from './nutrition-plan.model';

describe('NutritionPlan', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new NutritionPlan()).toBeTruthy();
  });
});
